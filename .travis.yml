language: python
python: 
  - "2.7"
cache: false
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
script:
  - pip install nose pylint
  #- nosetests && pylint --error-only
  - pip install -r requirements.txt
  - python -c "import tensorflow as tf; print(tf.__version__)"
  - python ml_bot/hello_world.py
  # this won't run without credentials
  #- python slack_bot/russ-bot.py
notifications:
  email:
    on_success: change
    on_failure: change
